FROM node:20-alpine

# Install system dependencies
RUN apk add --no-cache curl

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Create uploads directory
RUN mkdir -p uploads

# Copy source code
COPY . .

# Expose port
EXPOSE 3001

# Start application
CMD ["npm", "run", "dev"]